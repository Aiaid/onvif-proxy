# binaries
/onvif-proxy
/dist/
/out/

# local runtime config (contains camera credentials) — commit only the example
config.yaml
/config/

# go
*.test
*.out
vendor/

# node (web UI build toolchain; the built bundle under
# internal/web/static/dist IS committed, node_modules is not)
node_modules/

# editor / os
.DS_Store
.idea/
.vscode/
